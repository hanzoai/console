interface PlanSelectionModalProps {
  isOpen: boolean;
  onClose: () => void;
  orgId: string;
  currentSubscription?: any;
}

export const PlanSelectionModal = (_props: PlanSelectionModalProps) => null;
