name: Docker Build and Deploy to Hanzo Platform

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to deploy to"
        type: choice
        options:
          - production
          - staging
        required: true
        default: production

concurrency:
  group: docker-deploy-${{ github.ref }}
  cancel-in-progress: true

env:
  DOCKER_IMAGE: hanzoai/console
  REGISTRY: docker.io

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write  # required for KMS OIDC auth
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get version
        id: version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Load Docker Hub secrets from Hanzo KMS
        uses: hanzoai/universe/.github/actions/kms-action@main
        with:
          identity-id: ${{ vars.KMS_IDENTITY_ID }}
          project-id: ${{ vars.KMS_PROJECT_ID }}
          environment: production
          secret-path: /github-actions/docker
          kms-domain: https://kms.hanzo.ai

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ env.DOCKERHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.DOCKER_IMAGE }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}},value=${{ steps.version.outputs.version }}
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,prefix={{branch}}-

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./web/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            NEXT_PUBLIC_BUILD_ID=${{ github.sha }}
            NEXT_PUBLIC_HANZO_CLOUD_REGION=US

  deploy-production:
    needs: build
    if: (github.ref == 'refs/heads/main' && github.event_name == 'push') || (github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'production')
    runs-on: ubuntu-latest
    environment: production
    permissions:
      contents: read
      id-token: write  # required for KMS OIDC auth
    steps:
      - name: Load deploy secrets from Hanzo KMS
        uses: hanzoai/universe/.github/actions/kms-action@main
        with:
          identity-id: ${{ vars.KMS_IDENTITY_ID }}
          project-id: ${{ vars.KMS_PROJECT_ID }}
          environment: production
          secret-path: /github-actions/deploy
          kms-domain: https://kms.hanzo.ai

      - name: Trigger Hanzo Platform Deployment
        run: |
          curl -X POST \
            -H "Content-Type: application/json" \
            -H "User-Agent: Go-http-client/1.1" \
            -d '{
              "push_data": {
                "tag": "latest",
                "pusher": "${{ github.actor }}"
              },
              "repository": {
                "repo_name": "hanzoai/console"
              }
            }' \
            "https://platform.hanzo.ai/api/deploy/${{ env.PLATFORM_DEPLOY_TOKEN }}"

  deploy-staging:
    needs: build
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'staging'
    runs-on: ubuntu-latest
    environment: staging
    permissions:
      contents: read
      id-token: write  # required for KMS OIDC auth
    steps:
      - name: Load staging deploy secrets from Hanzo KMS
        uses: hanzoai/universe/.github/actions/kms-action@main
        with:
          identity-id: ${{ vars.KMS_IDENTITY_ID }}
          project-id: ${{ vars.KMS_PROJECT_ID }}
          environment: staging
          secret-path: /github-actions/deploy
          kms-domain: https://kms.hanzo.ai

      - name: Trigger Hanzo Platform Staging Deployment
        run: |
          curl -X POST \
            -H "Content-Type: application/json" \
            -H "User-Agent: Go-http-client/1.1" \
            -d '{
              "push_data": {
                "tag": "latest",
                "pusher": "${{ github.actor }}"
              },
              "repository": {
                "repo_name": "hanzoai/console"
              }
            }' \
            "https://platform.hanzo.ai/api/deploy/${{ env.PLATFORM_DEPLOY_TOKEN_STAGING }}"

  notify:
    needs: [build, deploy-production, deploy-staging]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Send notification
        if: github.event_name == 'push' && github.ref == 'refs/heads/main' && vars.SLACK_WEBHOOK != ''
        uses: 8398a7/action-slack@v3
        with:
          status: ${{ job.status }}
          text: |
            Console Deployment Status: ${{ job.status }}
            Version: ${{ needs.build.outputs.version }}
            Commit: ${{ github.sha }}
            Author: ${{ github.actor }}
            Message: ${{ github.event.head_commit.message }}
        env:
          SLACK_WEBHOOK_URL: ${{ vars.SLACK_WEBHOOK }}
